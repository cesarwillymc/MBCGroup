query GetSurveys($first: Int!) {
    surveys(first: $first) {
        edges {
            node {
                id
                title
                description
                coverImageUrl
            }
        }
        pageInfo {
            hasNextPage
        }
        totalCount
    }
}
